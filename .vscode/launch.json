{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Жопаноид JS (Run)",
      "type": "node-terminal",
      "request": "launch",
      "command": "node server.mjs",
      "cwd": "${workspaceFolder}"
    }
  ]
}
